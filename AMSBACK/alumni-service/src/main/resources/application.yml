server:
  port: 8082

spring:
  application:
    name: alumni-service
  datasource:
    url: jdbc:mysql://localhost:3306/darta?createDatabaseIfNotExist=true
    username: root
    password: makelawde
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
